#include "main.h"
#include <stdio.h>

/**
 * swap_int - swaps two integers
 * @a: first integer
 * @b: secong integer
*/

void swap_int(int *a, int *b)
{
	int *a;
	int *b;

	swap_int(&a, &b);
	_putchar("a=%d, b%d\n", a, b)
}
