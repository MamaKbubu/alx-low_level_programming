#include "main.h"
/**
 * _abs - compiles the absolute value of an integer
 * @d: the integer to be compiled
 * Return: The absolute value
*/

int _abs(int d)
{
	if (d >= 0)
		return (d);
	else
		return (-d);
}
