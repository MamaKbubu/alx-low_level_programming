#include "main.h"

/**
 * print_alphabet_x10 - print the first 10 alphabets
 *
*/

void print_alphabet_x10(void)
{
	char alpha;
		for (alpha = 'a'; alpha <= 'z' ; alpha++)
			_putchar(stderr, 'alpha' (stderr));
	_putchar('\n');
}
